

<html>
  <head>   

	<div id="center"> 
		<h1><%= Post.first(:order => "RANDOM()").title %>n</h1>
	<td></td>

		<table>
		  <tr>
		    <th>Title</th>
		    <th>Author</th>
		    <th></th>
		    <th></th>
		    <th></th>
		  </tr>

		<% @posts.each do |post| %>
		  <tr>
		 
		    <td><%= post.title %></td>
		    <td><%= post.author %></td>
		    <td><%= link_to 'Show', post %></td>
		    <td><%= link_to 'Edit', edit_post_path(post) %></td>
		   	<td><%= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :destroy %></td>
		  </tr>
		
	
		<% end %>
		</table>

		<br />

		<%= link_to 'New Post', new_post_path %>
	</div>

      <div id="plane1"></div>
      <div id="plane2"></div>
      <div id="logo"></div>
         
        </form>
      </div>
    </body>
  </head>
</html>